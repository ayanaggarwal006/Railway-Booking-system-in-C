#define size 10

//structure for users that stores signed in user details
struct userData
{
    char name[30];
    char userName[30];
    char password[30];
    long int phoneNumber;
};

struct userData user[size];